<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('main_home') }}">
            <img src="{{ asset('img/event.png') }}" alt="logo" width="35" height="35" class="d-inline-block align-text-top">
            Sortir.com
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
                aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <!-- TODO: Ajouter les chemins  -->
        <div class="collapse navbar-collapse d-lg-flex flex-lg-grow-0" id="navbarNavAltMarkup">
            <div class="navbar-nav d-lg-flex">
                {% if is_granted('ROLE_USER') %}
                    <a class="btn btn-outline-secondary me-lg-2 nav-link mb-1 mb-lg-0 mt-1 mt-lg-0" href="{{ path('main_home') }}"><i class="fa-solid fa-house"></i> Accueil</a>
                    <a class="btn btn-outline-secondary me-lg-2 nav-link mb-1 mb-lg-0" href="{{ path('sortie_creation') }}"><i class="fa-solid fa-plus"></i> Nouvelle Sortie</a>
                    <a class="btn btn-outline-secondary me-lg-2 nav-link mb-1 mb-lg-0" href="{{ path('participant_profil') }}"><i class="fa-solid fa-user"></i> Mon profil</a>
                    <a class="btn btn-outline-secondary me-lg-2 nav-link mb-1 mb-lg-0" href="{{ path('app_logout') }}"><i class="fa-solid fa-right-from-bracket"></i> Se déconnecter</a>
                {% endif %}
                {% if is_granted("ROLE_ADMIN") %}
                    <div class="dropdown">
                        <button class="btn btn-outline-secondary dropdown-toggle nav-link col-12" id="navbarScrollingDropdown" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="fa-solid fa-screwdriver-wrench"></i> Droits Administrateur
                        </button>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarScrollingDropdown">
                            <li><a class="dropdown-item" href="{{ path('ville_liste') }}">Villes</a></li>
                            <li><a class="dropdown-item" href="{{ path('campus_liste') }}">Sites</a></li>
                            <li><a class="dropdown-item" href="{{ path('participant_liste') }}">Utilisateurs</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <p class="ms-3 me-3"> Ajouter un utilisateur :</p>
                            <li><a class="dropdown-item" href="{{ path('registration_registerForm') }}">Formulaire</a></li>
                            <li><a class="dropdown-item" href="{{ path('registration_registerCSV') }}">Import par fichier</a></li>
                        </ul>
                    </div>

                {% endif %}
            </div>

        </div>
    </div>
</nav>