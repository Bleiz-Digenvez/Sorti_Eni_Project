<nav class="navbar navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">
            <img src="{{ asset('img/event.png') }}" alt="logo" width="35" height="35" class="d-inline-block align-text-top">
            Sortir.com
        </a>
        <!-- TODO: Ajouter les chemins  -->
        <div class="d-flex">
            {% if is_granted('ROLE_USER') %}
            <a class="btn btn-outline-secondary me-2" href="{{ path('main_home') }}">Accueil</a>
            <a class="btn btn-outline-secondary me-2" href="{{ path('sortie_creation') }}">Nouvelle Sortie</a>
            <a class="btn btn-outline-secondary me-2"href="{{ path('participant_profil') }}">Mon profil</a>
            <a class="btn btn-outline-secondary me-2"href="{{ path('app_logout') }}">Se déconnecter</a>
            {% endif %}
            {% if is_granted("ROLE_ADMIN") %}
                <div class="dropdown">
                    <button class="btn btn-outline-secondary dropdown-toggle" id="navbarScrollingDropdown" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Droits Administrateur
                    </button>
                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarScrollingDropdown">
                        <li><a class="dropdown-item" href="{{ path('ville_liste') }}">Villes</a></li>
                        <li><a class="dropdown-item" href="#">Sites</a></li>
                        <li><a class="dropdown-item" href="#">Utilisateurs</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <p class="ms-3 me-3"> Ajouter un utilisateur :</p>
                        <li><a class="dropdown-item" href="{{ path('registration_registerForm') }}">Formulaire</a></li>
                        <li><a class="dropdown-item" href="{{ path('registration_registerCSV') }}">Import par fichier</a></li>
                    </ul>
                </div>

            {% endif %}
        </div>
    </div>
</nav>